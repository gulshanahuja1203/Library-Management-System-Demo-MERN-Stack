To run this project first clone all the files to your machine
Open this folder in visual studio and run "npm install" command in the terminal to get the node_modules
run the project using the command "npm start" in the terminal
